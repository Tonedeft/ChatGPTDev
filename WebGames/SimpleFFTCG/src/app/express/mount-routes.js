// This file is the route mounter for serving URL addresses
// so '/' routes to config.homeApp.router
// 'record-viewing' routes to config.recordViewingsApp.router


function mountRoutes(app, config) {
    // app.use('/', config.homeApp.router)
    // app.use('/record-viewing', config.recordViewingsApp.router)
}

module.exports = mountRoutes
